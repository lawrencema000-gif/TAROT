logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Credential documentation — should not be in repo
android/google-oauth-credentials.md
android/keystore-fingerprints.md

# Google OAuth credential files
android/app/client_secret_*.json
android/app/gemini_*.json
*.keystore
*.jks

# Local audit / scratch / secrets — never commit. Service account JSON
# keys and other local diagnostic files live here and must stay out of
# version control.
.audit/secrets/
.audit/secrets/**

# Local Netlify folder
.netlify
.vercel
